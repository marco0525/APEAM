
<h1>
    Producto
</h1>
<div class="container">
    <h2>Agregar Producto</h2>
    <form method="post" enctype="multipart/form-data" asp-controller="Productos" asp-action="Create">
        <div class="form-group">
            <label for="Name">Nombre de Producto:</label>
            <input type="text" class="form-control" id="Name" placeholder="Ingresar Nombre de producto" name="Name">
        </div>
        @*<div class="form-group">
            <label for="idProveedor">Seleccionar Proveedor:</label>
            <select class="form-control" id="proveedor">
                @foreach (var item in ViewBag.Proveedores)
                {
                    <option value="@item.Id">@item.Name</option>
                }
            </select>
        </div>*@
        <br />
        <button type="submit" class="btn btn-primary">Agregar</button>
    </form>
</div>